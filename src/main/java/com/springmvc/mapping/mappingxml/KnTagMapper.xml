<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springmvc.mapping.KnTagMapper">
  <resultMap id="BaseResultMap" type="com.springmvc.pojo.KnTag">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Mar 05 18:04:04 CST 2019.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="img" jdbcType="VARCHAR" property="img" />
    <result column="add_time" jdbcType="TIMESTAMP" property="addTime" />
  </resultMap>


  <!--自定义用户信息表分页.boyang-->
  <select id="queryListTag" parameterType="kn_tag" resultType="com.springmvc.pojo.kn_tag">
    select id,title,img,add_time from kn_tag
    <where>
      <if test="title!=null and title!='' ">
        title LIKE '%${title}%'
      </if>
    </where>
    ORDER  BY add_time
  </select>

  <!-- 标签增加 -->
  <insert id="MercjatTagIncrease" parameterType="kn_tag">
    INSERT INTO kn_tag (title,add_time) VALUES(#{title},#{addTime});
  </insert>
  <!-- 标签编辑 -->
  <update id="MercjatTagUpadete" parameterType="kn_tag">
    UPDATE kn_tag SET title=#{title} WHERE id=#{id}
  </update>
  <!-- 标签删除 -->
  <delete id="MercjatTagDelete">
  delete FROM kn_tag where id=#{id}
  </delete>

  <!--查询标签信息-->
  <select id="selectByidTag" parameterType="int" resultType="kn_tag">
    SELECT * FROM kn_tag WHERE id=#{id}
   </select>

</mapper>